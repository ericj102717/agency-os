# Render Blueprint for Agency OS Backend
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: agency-os-backend
    runtime: python
    rootDir: .
    buildCommand: pip install -r backend/requirements.txt
    startCommand: python3 backend/server.py
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        sync: postgresql://postgres.Uo7HUK00Eaju4YJ7@db.jpeavskedjffubzojdmu.supabase.co:5432/postgres
      - key: ALLOWED_ORIGINS
        sync: 
      - key: PORT
        value: "10000"
      - key: PYTHON_VERSION
        value: "3.11.0"
